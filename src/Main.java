public class Main {
    public static void main(String[] args) {

        LoginPanel Home=new LoginPanel();
        //Cliente Home =new Cliente();
        Home.setVisible(true);

        /*
        JFrame frame = new JFrame("Sistema de Facturación");
        frame.setSize(300, 200);
        frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
        frame.setLayout(new GridLayout(2, 1));

        JButton btnCreaCliente = new JButton("Crear Cliente");
        JButton btnNuevaFactura = new JButton("Nueva Factura");

        btnCreaCliente.addActionListener(e -> new RegistrCliente().setVisible(true));
        btnNuevaFactura.addActionListener(e -> new FacturaNueva().setVisible(true));

        frame.add(btnCreaCliente);
        frame.add(btnNuevaFactura);

        frame.setVisible(true);


         */
    }
}